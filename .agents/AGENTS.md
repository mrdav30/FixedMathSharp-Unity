# AGENTS.md

## Repo Scope

- The actual Git repo root is the `Assets/Packages` directory, not the outer Unity project root.
- This repo ships the Unity package `com.mrdav30.fixedmathsharp` and `com.mrdav30.fixedmathsharp.lean`
  - Currently at version `4.0.0`.
